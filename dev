#!/bin/sh
set -eu

cd "$(dirname "$0")"

if ! curl --silent --fail --max-time 1 http://127.0.0.1:11434/api/tags >/dev/null; then
  echo "Ollama is not running. The app will work, but AI requests will fail until Ollama starts."
fi

exec npm --prefix app run dev -- --hostname 127.0.0.1
